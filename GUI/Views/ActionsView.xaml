<ScrollViewer xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              VerticalScrollBarVisibility="Auto"
              HorizontalScrollBarVisibility="Disabled">

    <StackPanel MaxWidth="700" Margin="0,0,0,24">

        <!-- ── Header ──────────────────────────────────────────── -->
        <TextBlock Text="Actions"
                   Style="{DynamicResource HeaderLabel}"
                   Margin="0,0,0,16" />

        <!-- ── Action + Mode row ───────────────────────────────── -->
        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Action"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           FontSize="12"
                           Margin="0,0,0,4" />
                <ComboBox x:Name="cmbAction"
                          Style="{DynamicResource ModernComboBox}" />
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Mode"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           FontSize="12"
                           Margin="0,0,0,4" />
                <ComboBox x:Name="cmbMode"
                          Style="{DynamicResource ModernComboBox}" />
            </StackPanel>
        </Grid>

        <!-- ── Toggle switches card ────────────────────────────── -->
        <Border Style="{DynamicResource CardBorder}" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal"
                            VerticalAlignment="Center" Margin="0,0,8,0">
                    <ToggleButton x:Name="tglNonInteractive"
                                  Style="{DynamicResource ToggleSwitch}"
                                  IsChecked="True" />
                    <TextBlock Text="Non-Interactive"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               FontSize="13"
                               VerticalAlignment="Center"
                               Margin="8,0,0,0" />
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal"
                            VerticalAlignment="Center" Margin="0,0,8,0">
                    <ToggleButton x:Name="tglForce"
                                  Style="{DynamicResource ToggleSwitch}" />
                    <TextBlock Text="Force"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               FontSize="13"
                               VerticalAlignment="Center"
                               Margin="8,0,0,0" />
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <ToggleButton x:Name="tglDryRun"
                                  Style="{DynamicResource ToggleSwitch}" />
                    <TextBlock Text="Dry Run"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               FontSize="13"
                               VerticalAlignment="Center"
                               Margin="8,0,0,0" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- ── Advanced Options expander ───────────────────────── -->
        <Expander x:Name="expAdvanced"
                  Header="Advanced Options"
                  Foreground="{DynamicResource TextPrimaryBrush}"
                  FontSize="14"
                  Margin="0,0,0,16">
            <Border Style="{DynamicResource CardBorder}" Margin="0,8,0,0">
                <StackPanel>
                    <!-- Toggle row: RemoveNetwork + CoreOnly -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal"
                                    VerticalAlignment="Center">
                            <ToggleButton x:Name="tglRemoveNetwork"
                                          Style="{DynamicResource ToggleSwitch}" />
                            <TextBlock Text="Remove Network"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       FontSize="13"
                                       VerticalAlignment="Center"
                                       Margin="8,0,0,0" />
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal"
                                    VerticalAlignment="Center">
                            <ToggleButton x:Name="tglCoreOnly"
                                          Style="{DynamicResource ToggleSwitch}"
                                          IsChecked="True" />
                            <TextBlock Text="Core Only"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       FontSize="13"
                                       VerticalAlignment="Center"
                                       Margin="8,0,0,0" />
                        </StackPanel>
                    </Grid>

                    <!-- Profile Path -->
                    <TextBlock Text="Profile Path"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               FontSize="12"
                               Margin="0,0,0,4" />
                    <TextBox x:Name="txtProfilePath"
                             Style="{DynamicResource ModernTextBox}"
                             Margin="0,0,0,12" />

                    <!-- Defaults File -->
                    <TextBlock Text="Defaults File"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               FontSize="12"
                               Margin="0,0,0,4" />
                    <TextBox x:Name="txtDefaultsFile"
                             Style="{DynamicResource ModernTextBox}"
                             Margin="0,0,0,12" />

                    <!-- Target Hosts -->
                    <TextBlock Text="Target Hosts (comma-separated)"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               FontSize="12"
                               Margin="0,0,0,4" />
                    <TextBox x:Name="txtTargetHosts"
                             Style="{DynamicResource ModernTextBox}"
                             Margin="0,0,0,12" />

                    <!-- Confirmation Token -->
                    <TextBlock Text="Confirmation Token"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               FontSize="12"
                               Margin="0,0,0,4" />
                    <TextBox x:Name="txtConfirmationToken"
                             Style="{DynamicResource ModernTextBox}" />
                </StackPanel>
            </Border>
        </Expander>

        <!-- ── Command preview ─────────────────────────────────── -->
        <TextBlock Text="Command Preview"
                   Foreground="{DynamicResource TextSecondaryBrush}"
                   FontSize="12"
                   Margin="0,0,0,4" />
        <TextBox x:Name="txtCommandPreview"
                 Style="{DynamicResource ModernTextBox}"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 MinHeight="60"
                 Margin="0,0,0,20" />

        <!-- ── Run button ──────────────────────────────────────── -->
        <Button x:Name="btnRunAction"
                Content="Run"
                Style="{DynamicResource ModernButton}"
                Width="160"
                Height="40"
                FontSize="15"
                HorizontalAlignment="Left" />

    </StackPanel>
</ScrollViewer>
