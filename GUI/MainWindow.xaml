<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OpenCodeLab"
        Width="1200" Height="800"
        MinWidth="900" MinHeight="600"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- ── Sidebar ────────────────────────────────────────────── -->
        <Border Grid.Column="0"
                Background="{DynamicResource SidebarBrush}">
            <DockPanel LastChildFill="True">

                <!-- App title (top) -->
                <TextBlock DockPanel.Dock="Top"
                           Text="OpenCodeLab"
                           FontSize="16" FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           Margin="16,16,16,24" />

                <!-- Theme toggle (bottom) -->
                <StackPanel DockPanel.Dock="Bottom"
                            Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Margin="0,0,0,16">
                    <TextBlock Text="&#x2600;"
                               FontSize="14"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,8,0" />
                    <ToggleButton x:Name="btnThemeToggle"
                                  Style="{DynamicResource ToggleSwitch}" />
                    <TextBlock Text="&#x263D;"
                               FontSize="14"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Margin="8,0,0,0" />
                </StackPanel>

                <!-- Navigation buttons (middle, fills remaining space) -->
                <StackPanel Margin="0,0,0,0">
                    <Button x:Name="btnNavDashboard"
                            Content="&#x25A0;  Dashboard"
                            Style="{DynamicResource SidebarButton}" />
                    <Button x:Name="btnNavActions"
                            Content="&#x25B6;  Actions"
                            Style="{DynamicResource SidebarButton}" />
                    <Button x:Name="btnNavCustomize"
                            Content="&#x270E;  Customize"
                            Style="{DynamicResource SidebarButton}" />
                    <Button x:Name="btnNavLogs"
                            Content="&#x2261;  Logs"
                            Style="{DynamicResource SidebarButton}" />
                    <Button x:Name="btnNavSettings"
                            Content="&#x2699;  Settings"
                            Style="{DynamicResource SidebarButton}" />
                </StackPanel>

            </DockPanel>
        </Border>

        <!-- ── Content area ───────────────────────────────────────── -->
        <Border Grid.Column="1"
                Background="{DynamicResource BackgroundBrush}">
            <Grid x:Name="contentArea" Margin="24">
                <TextBlock x:Name="txtPlaceholder"
                           Text="Loading..."
                           FontSize="16"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center" />
            </Grid>
        </Border>

    </Grid>
</Window>
